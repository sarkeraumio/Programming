#include <stdio.h>
int main(void){
    // Here your code !
    
    int a, b;
    printf("enter the intergers a and b");
    scanf("%d%d", &a,&b);
    if(a==b)
    printf(" Number1 and Number2 are equal ");
    else
    printf("Number1 and Number2 arenot  equal");
    
    return 0;
    
}
